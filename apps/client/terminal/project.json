{
  "name": "client-terminal",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/client/terminal/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/client/terminal"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "client-terminal:build",
        "proxyConfig": "apps/client/terminal/proxy.conf.json"
      },
      "configurations": {
        "development": {
          "buildTarget": "client-terminal:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "client-terminal:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "client-terminal:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "client-terminal:build:development"
        },
        "production": {
          "buildTarget": "client-terminal:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/apps/client/terminal"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../../coverage/apps/client/terminal"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/client/terminal/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "serve-static": {
      "executor": "@nrwl/web:file-server",
      "options": {
        "buildTarget": "client-terminal:build"
      }
    }
  },
  "tags": []
}
